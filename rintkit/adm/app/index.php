<?php
//-------------------------------------------------------------------------------------------------
/*
 * 어플리케이션 관리자 연동
 */
//-------------------------------------------------------------------------------------------------

require_once "../engine.php";

//-----------------------------------------------------------------------------------------

/**
 * APP 등록정보 설정
 */

$_app = $dbo->fetch("SELECT * FROM RT_APP WHERE app_code='".$env->_get['appcode']."'");

//-----------------------------------------------------------------------------------------

/* APP 관리자 연결 및 데이터 세팅 */
$app_admin_path = RT_DOC_ROOT.$_app['app_path']."/".$__sd."/index.php";
$app_admin_path = str_replace("//", "/", $app_admin_path);
if (is_file($app_admin_path) === TRUE) {
	require_once $app_admin_path;
}

//-----------------------------------------------------------------------------------------

/**
 *레이아웃 / 테마 설정
 */

$__app_admin = "y";

require_once RT_LAYOUT."/layout.php";

//-----------------------------------------------------------------------------------------
?>